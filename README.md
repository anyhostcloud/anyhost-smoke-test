# anyhost-smoke-test

Minimal Dockerfile-based service used to verify the anyhost dev deployment
path.

- `GET /health` returns a JSON health response.
- `GET /` returns a short plaintext response.
- The container listens on port `8080`.
Smoke test repository for anyhost GitHub App onboarding.
